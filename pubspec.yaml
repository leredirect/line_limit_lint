name: line_limit_lint
description: "Lint rule, designed to highlight widget files that exceed a specified size threshold."
version: 0.0.6
homepage: https://github.com/leredirect/line_limit_lint
repository: https://github.com/leredirect/line_limit_lint

environment:
  sdk: '>=3.2.6 <4.0.0'

dependencies:
  analyzer: ^6.4.1
  analyzer_plugin: ^0.11.3
  custom_lint_builder: ^0.6.0
